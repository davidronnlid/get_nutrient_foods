const Comp = (stateProps) => {
  console.log(stateProps);
  return (
    <>
      {/* {stateProps.stateProps.map((elm) => (
        <p>{elm}</p>
      ))} */}
      Hey
    </>
  );
};

export default Comp;
